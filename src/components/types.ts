export type ProgressProps = {
  itemsCount: number;
  checkedItemsCount: number;
};

export type HeroProps = {
  itemsCount: number;
  checkedItemsCount: number;
  clearItems: Function;
};

export type BagInfoModalProps = {
  open: boolean;
  setOpen: Function;
};
